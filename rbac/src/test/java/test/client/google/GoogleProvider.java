package test.client.google;

import test.client.ClientProvider;
import test.model.Tool;

public class GoogleProvider implements ClientProvider<Google> {

    public Google create(Tool tool) {
        return null;
    }
}